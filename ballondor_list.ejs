<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>バロンドール歴代受賞者</title>
  <style>
    body { font-family: "Helvetica Neue", Arial, sans-serif; padding: 20px; color: #333; background-color: #f9f9f9; }
    h1 { color: #d4af37; border-bottom: 3px solid #d4af37; padding-bottom: 10px; } /* ゴールドカラー */
    table { width: 100%; max-width: 800px; border-collapse: collapse; margin-top: 20px; background-color: white; }
    th, td { border: 1px solid #ddd; padding: 12px; text-align: left; }
    th { background-color: #333; color: #d4af37; }
    tr:nth-child(even) { background-color: #f2f2f2; }
    a { color: #0056b3; text-decoration: none; font-weight: bold; }
    a:hover { text-decoration: underline; }
  </style>
</head>
<body>
  <h1>バロンドール歴代受賞者 (抜粋)</h1>
  
  <table>
    <tr>
      <th>年度</th>
      <th>受賞者名</th>
      <th>国籍</th>
      <th>所属クラブ</th>
    </tr>
    <% winners.forEach((winner, index) => { %>
      <tr>
        <td><%= winner.year %></td>
        <!-- 名前をクリックすると詳細ページへ -->
        <td><a href="/ballondor/<%= index %>"><%= winner.name %></a></td>
        <td><%= winner.country %></td>
        <td><%= winner.club %></td>
      </tr>
    <% }); %>
  </table>

  <br>
  <a href="/">トップに戻る</a>
</body>
</html>