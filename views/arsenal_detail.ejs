<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>選手詳細 - <%= player.name %></title>
  <style>
    body { font-family: sans-serif; padding: 20px; color: #333; }
    table { border-collapse: collapse; width: 100%; max-width: 500px; margin-bottom: 20px; }
    th, td { border: 1px solid #ccc; padding: 12px; text-align: left; }
    th { background-color: #f4f4f4; width: 35%; }
    .btn { padding: 10px 20px; text-decoration: none; border-radius: 4px; display: inline-block; font-size: 0.9em; cursor: pointer; }
    .btn-edit { background-color: #007bff; color: white; margin-right: 10px; border: none; }
    .btn-back { background-color: #6c757d; color: white; }
    .btn-delete { background-color: #dc3545; color: white; border: none; }
    .actions { margin-top: 20px; border-top: 1px solid #eee; padding-top: 20px; }
  </style>
</head>
<body>
  <h2>選手詳細情報</h2>
  
  <table>
    <tr><th>名前</th><td><%= player.name %></td></tr>
    <tr><th>ポジション</th><td><%= player.position %></td></tr>
    <tr><th>年齢</th><td><%= player.age %> 歳</td></tr>
    <tr><th>身長</th><td><%= player.height %> cm</td></tr>
    <tr><th>利き足</th><td><%= player.foot %></td></tr>
    <tr><th>評価</th><td><%= player.rating %></td></tr>
  </table>

  <div class="actions">
    <a href="/arsenal/edit/<%= index %>" class="btn btn-edit">編集する</a>

    <form action="/arsenal/delete/<%= index %>" method="POST" style="display:inline;" onsubmit="return confirm('この選手を削除してもよろしいですか？');">
      <button type="submit" class="btn btn-delete">削除する</button>
    </form>

    <a href="/arsenal" class="btn btn-back">一覧に戻る</a>
  </div>

</body>
</html>