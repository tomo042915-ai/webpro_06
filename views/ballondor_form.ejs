<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>受賞者情報入力</title>
  <style>
    body { font-family: sans-serif; padding: 20px; color: #333; }
    .form-group { margin-bottom: 15px; }
    label { display: block; margin-bottom: 5px; font-weight: bold; }
    input, textarea { padding: 8px; width: 100%; max-width: 400px; box-sizing: border-box; }
    .btn { padding: 10px 20px; background: #333; color: white; border: none; cursor: pointer; }
  </style>
</head>
<body>
  <h2><%= mode === 'create' ? '新規受賞者登録' : '受賞者情報編集' %></h2>
  
  <form action="<%= mode === 'create' ? '/ballondor/add' : '/ballondor/update/' + index %>" method="POST">
    <div class="form-group">
      <label>受賞年度</label>
      <input type="number" name="year" value="<%= winner ? winner.year : '' %>" required>
    </div>
    <div class="form-group">
      <label>名前</label>
      <input type="text" name="name" value="<%= winner ? winner.name : '' %>" required>
    </div>
    <div class="form-group">
      <label>国籍</label>
      <input type="text" name="country" value="<%= winner ? winner.country : '' %>">
    </div>
    <div class="form-group">
      <label>当時所属クラブ</label>
      <input type="text" name="club" value="<%= winner ? winner.club : '' %>">
    </div>
    <div class="form-group">
      <label>ポジション</label>
      <input type="text" name="position" value="<%= winner ? winner.position : '' %>">
    </div>
    <div class="form-group">
      <label>受賞理由・備考</label>
      <textarea name="desc" rows="4"><%= winner ? winner.desc : '' %></textarea>
    </div>
    <button type="submit" class="btn">保存</button>
  </form>
  
  <p><a href="/ballondor">一覧に戻る</a></p>
</body>
</html>